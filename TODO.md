* 通过系统全局快捷键打开本软件。
* 可以点击或用空格键计数；再次点击/Space以取消或用Ctrl+Z撤销上一步。
* 点击放置参考点按钮,可以放置参考点标记，可以选择点、空心圆、十字等。
* 按住Alt可以临时减小标记点大小；滚轮调节标记点大小。
* 透明标记环跟随鼠标，指示标记点大小。
* 按住Ctrl+滚轮在鼠标位置调节图像大小，按住~可以临时放大一定倍数。
* Ctrl+S 保存标记到文件。
* Ctrl+Shift+S 保存当前标记截图和标记文件，自动记屏幕左上角为主要参考点。
* 计数模式(对应编辑模式)下，可以按T进入方框框选或按Y用任意形状框选，框选完就可以输入计数，
  按Enter或点击框外即可退出输入。

* 可以导出标记点相对位置，但需要确定原点坐标。
* 创建接口，方便被第三方软件调用。

* 考虑是否需要使用 GPL v3 或 AGPL v3 来替换 GPL v2。 MIT 许可证也是一个选项。

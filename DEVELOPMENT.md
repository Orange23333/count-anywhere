# Branches

After we released the first release, the development version will not be directly merged into the main branch.

# Project Information
## Version Code

- `release` like: `major`.`minor`.`build`
- `dev` like: `major`.`minor`.`build`a`revision`

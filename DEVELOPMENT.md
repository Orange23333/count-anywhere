# Project Information
## Version Code

- Release like: `major`.`minor`.`build` or `major`.`minor`.`build`.`revision`
- Test like: `major`.`minor`.`build`a`revision`.dev`date`
  - `revision` 是用于确保版本号递增的。
# 相关文件及注解

*未提及的部分请勿访问。*

* `.github/*`：*暂不考虑。*
* `assets/*`：资源文件。
* `installer/*`：*暂不考虑。*
* `src/count_anywhere/`：
  * `__init__.py`：需要时可添加代码。
  * `config.yml`：默认配置，需要时可添加代码。
  * `main.py`：雏形已完成，需要时可添加代码。
  * `rc_resources.py`：**由pyside的rcc工具自动生成，不要动！**
  * `sgt_dns.py`：需要时可添加代码。
  * `libs/`：
    * `__init__.py`：需要时可添加代码。
    * `collections.py`：已完成。
    * `configs.py`：已完成。
    * `data_bindings.py`：*暂不考虑。*
    * `hotkeys.py`：*暂不考虑。*
    * `i18n.py`：已完成。
    * `io.py`：已完成。
    * `markers.py`：已完成。
    * `path_dict.py.del`：*暂不考虑。*
    * `qt_ext.py`：*暂不考虑。*
    * `utils.py`：已完成。
    * `win32.py`：*已完成，但暂时不用。*
  * `locales/*`：翻译，需要时可更新。
  * `widgets/`：
    * `__init__.py`：需要时可添加代码。
    * `about_window.py`：已完成。
    * `AboutWidget.qml.del`：*本来准备用QtQuick+QML实现的，但是放弃了。暂不考虑。*
    * `config_window.py`：已完成。
    * `marker_editor.py`：未完成。
    * `result_table_dialog.py`：未完成。
    * `system_tray.py`：已完成。
* `tests/*`：*暂不考虑。*
* `tools/*`：*暂不考虑。*
* `.coveragerc`：*暂不考虑。*
* `[FAQ] About @property.md`：已完成，无需修改。
* `About Origin.txt`：已完成，无需修改。
* `PROJECT_INDEX.md`：本文件。用于列出可被AI使用的文件或目录，并进行简单注解和进度标记的文件。请在增，删，移动和重命名可被AI使用的文件后在这里修改。
* `pyproject.toml`：已完成，需要时可添加代码。
* `LICENSE`：许可证。检查copyright声明年份段即可。
* `README.md`：尚待完善。
* `requirements.dev.txt`：已完成，需要时可添加代码。
* `requirements.txt`：已完成，需要时可添加代码。
* `resources.qrc`：已完成，需要时可添加代码。
* `ruff.toml`：*暂不考虑。*
* `TODO.md`：人为记录的事项清单，AI 请无修改。